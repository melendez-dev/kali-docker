# kali-docker | wifi | hackTheBox

### Guide

```
chmod +x install.sh && chmod +x depedences.sh
./install.sh
````
This install and build the image and put you inside the container.

exit and run 

````
./depedences.sh
````
This will install the common tools in kali linux
